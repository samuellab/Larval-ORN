load(fullfile('.', 'data', 'AveRawDataMatrix2ndRound.mat'));

%%
for i = 20:length(odorList)
    for j = 1:length(ORNList)
        resp = dataMean(i, j, :);
        if sum(resp) ~= 0
            resp_sem = dataSEM(i, j, :);
            figure;
            errorbar(log(concList, resp(:), resp_sem(:));
            title([odorList{i}, num2str(i), ORNList{j}, num2str(j) ]);
        end
    end
end